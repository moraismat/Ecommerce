spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ecommerce
    username: root
    password: admin

  sql:
    init: always

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
